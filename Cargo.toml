[package]
name = "polyblade"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "pblade"
path = "src/main.rs"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cgmath = { version = "^0.18", features = ["swizzle"] }
ndarray = "0.15.6"
rand = "^0.7"
three-d = { version = "^0.17", default-features = false, features = [
	"window",
	"egui-gui",
] }
