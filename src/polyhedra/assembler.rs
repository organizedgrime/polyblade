// Keep track of many states while an operation is being performed
pub struct Assembler {
    // pub flags
    // pub vertices
    // pub vert
    // pub faces
}

impl Assembler {
    /*
    pub fn commit() -> Polyhedron {
    }
    */
}
